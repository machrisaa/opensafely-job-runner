# This project is invalid because it contains a variable that refers
# to a non-existent output
version: '1.0'

actions:

  generate_cohorts:
    run: cohortextractor
    outputs:
      cohort: input.csv

  run_model:
    run: stata-mp:latest analysis/model.do ${{ needs.frob.outputs.x }}
    needs: [generate_cohorts]
    outputs:
      log: model.log
